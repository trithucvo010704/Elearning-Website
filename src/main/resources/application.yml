server:
  port: 8080
  tomcat:
    connection-timeout: 60000
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/elearning?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Trithuc23@
  jpa:
    hibernate:
      ddl-auto: update # Thay đổi từ validate → update để tự động tạo/update bảng
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true # Format SQL cho dễ đọc

app:
  jwt:
    secret: ${JWT_SECRET:change-this-very-long-secret-string-1234567890} # >= 32 bytes
    expirySeconds: 86400
  video:
    provider: manual # manual | youtube
    allowed-origins:
      - http://localhost:3000
      - http://localhost:8080
      - https://your-domain.com
